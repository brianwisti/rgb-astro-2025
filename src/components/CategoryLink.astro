---
import TaxonomyLink from './core/TaxonomyLink.astro'

interface Props {
    category: string
}
const { category } = Astro.props
---

<TaxonomyLink
    class="bg-gold-800 font-bold"
    title={`category: ${category}`}
    href={`/categories/${category}/`}
>{category}</TaxonomyLink>

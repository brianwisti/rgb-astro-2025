---
interface Props {
    description: string
}

const { description } = Astro.props as Props
---
<p>{description}</p>

<style>
    p {
      font-size: var(--font-size-smaller);
      font-style: italic;
      padding: .5em 0 0 1em;
    }
</style>

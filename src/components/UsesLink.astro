---
import TaxonomyLink from "./core/TaxonomyLink.astro"

interface Props {
    used: string
}

const { used } = Astro.props as Props
---

<TaxonomyLink
    href={`/uses/${used}/`}
    class="font-semibold"
    title={`uses: ${used}`}><slot>{used}</slot></TaxonomyLink>

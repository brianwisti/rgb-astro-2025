---
import PCategoryLink from "./core/PCategoryLink.astro";

interface Props {
    tag: string;
}

const { tag } = Astro.props;
---

<PCategoryLink href={`/tags/${tag}/`} classNames="tag">{tag}</PCategoryLink>

---
import TaxonomyLink from "./core/TaxonomyLink.astro"

interface Props {
    tag: string
}

const { tag } = Astro.props as Prop
const href = `/tags/${tag}/`
---

<TaxonomyLink
    class="before:content-['#'] italic"
    title={`tag: ${tag}`}
    {href}><slot>{tag}</slot></TaxonomyLink>

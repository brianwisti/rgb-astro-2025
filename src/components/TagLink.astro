---
import TaxonomyLink from "./core/TaxonomyLink.astro";

interface Props {
    tag: string;
}

const { tag } = Astro.props;
---

<TaxonomyLink href={`/tags/${tag}/`} classNames="tag">{tag}</TaxonomyLink>
